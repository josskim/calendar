# 2026-08 변경 이력

## 2026-08-01 — 마지막 예약 저장 후 3분 동기화 대기

- 요청 원문: "2박,3박일 경우도 있어.. 그냥 등록후 몇분후에 신규로 등록된 예약을 모두 한꺼번에 워커에 전달 하는게 낫지 않을까? 예를들어 3분? 후.. 오케이 수정해줘."
- 결과 요약:
  - 예약 생성·수정·취소 이벤트를 즉시 전달하지 않고 마지막 저장 후 3분 뒤 처리 가능하도록 예약했다.
  - 3분 안에 다른 예약 변경이 추가되면 아직 대기 중인 이벤트 전체의 처리 시각을 마지막 변경 기준으로 다시 연장한다.
  - 호실별로 여러 번 저장하거나 2박·3박 예약을 입력하는 중간 상태가 외부 사이트에 전송되지 않고 최종 캘린더 상태가 전달된다.
- 변경 파일:
  - `calapp/lib/inventory-events.ts`
- 검증:
  - `npm.cmd run build` 성공 (Next.js 프로덕션 빌드 및 TypeScript 검사)
- 구현 커밋: `133830e`

## 2026-08-01 — 예약 취소 시 외부 재고 해제 이벤트 보장

- 요청 원문: "staysync 앱, 캘린더 사이트로 PC나 모바일로 직접등록, 이 두가지 방법으로 캘린더에 예약을 등록하고있어. 이 2가지 방법으로 캘린더에 예약 등록 및 등록된 예약이 취소로 변경 저장되면.. 각 사이트에 예약 차단 및 예약 허용을 해주는 테스트를 할꺼야. 이제 워크등 완전히 기능 다 되도록 진행하자.. 다 준비되면 내가 테스트 데이터 입력해 볼께."
- 결과 요약:
  - StaySync 등록은 예약 생성과 같은 트랜잭션에서 `reservation.created` 이벤트를 생성하는 기존 경로를 확인했다.
  - PC·모바일 CalApp 직접 등록도 예약 생성과 같은 트랜잭션에서 `reservation.created` 이벤트를 생성하는 기존 경로를 확인했다.
  - 직접 등록 예약을 취소로 저장할 때 불필요한 중복 예약 검사가 취소 처리를 막지 않도록 수정했다.
  - 취소 저장은 변경 전·후 스냅샷을 포함한 `reservation.cancelled` 이벤트를 생성하므로 워커가 이전 날짜·호실의 판매를 다시 열 수 있다.
- 변경 파일:
  - `calapp/app/api/admin/reservations/route.ts`
- 검증:
  - `npm.cmd run build` 성공 (Next.js 프로덕션 빌드 및 TypeScript 검사)
  - GitHub `main` 푸시 완료, 운영 배포 대상에 반영
- 구현 커밋: `6d15ebe`

## 2026-08-01 — 오늘 이후 4개 예약 사이트 판매 상태 검증 보고서

- 요청 원문: "캘린더에 있는 오늘 이후 11월까지 예약과 4개사이트의 예약 마감/차단을 비교해서 결과를 보고하는 프로그램을 만들고, CalApp에 '오늘 이후 예약 검증' 버튼과 페이지 보고서를 만들어 달라. 캘린더 예약 마감인데 허용이거나 CalApp은 예약 가능한데 외부 사이트가 예약된 항목을 찾아 달라."
- 추가 요청: "날짜·객실별 확인 대신 월 화면을 한 번에 읽고, 네이버는 주간 화면을 사용한다. 토요일은 201호·202호 단독 상품을 임의로 막고 201호+202호 상품만 예약 가능하게 운영하는 규칙을 참고한다. 에어비앤비는 달력에서 11월까지 스크롤할 수 있다."
- 결과 요약:
  - 캘린더에 `오늘 이후 예약 검증` 버튼과 별도 읽기 전용 보고서 페이지를 추가했다.
  - 지정 기간의 CalApp 예약과 네이버·야놀자·여기어때·에어비앤비 상태를 정상·중요·확인·오류로 분류한다.
  - 검증 항목을 사이트·월 단위로 워커에 전달해 날짜별 브라우저 재접속을 제거했다.
  - 네이버 토요일 단독 상품 마감은 정상 운영 정책으로 판정하고, 단독 상품이 열리거나 묶음 상품이 잘못 열리면 보고한다.
  - 실제 2026-08-01~2026-11-30 보고서 `#2`를 운영 환경에서 실행했다.
  - 최종 1,464건을 모두 처리했으며 정상 1,464건, 중요·확인·오류 0건이었다.
  - 야놀자 2026-08-15 101호 `예약완료`는 외부 예약으로 판독했고 CalApp의 `nol` 예약과 정상 일치했다.
- 변경 파일:
  - `calapp/app/admin/(dashboard)/calendar/page.tsx`
  - `calapp/app/admin/(dashboard)/inventory-audit/page.tsx`
  - `calapp/app/admin/(dashboard)/NavLinks.tsx`
  - `calapp/app/api/admin/inventory-audits/**`
  - `calapp/app/api/worker/inventory-audits/**`
  - `calapp/lib/inventory-audits.ts`
  - `calapp/prisma/schema.prisma`
  - `calapp/prisma/migrations/20260801161000_add_inventory_audits/migration.sql`
  - `calapp/docs/inventory-audit.md`
  - `docs/changes/assets/2026-08-01-naver-weekly-saturday-policy.png`
- 검증:
  - `npm.cmd run build`: 프로덕션 빌드 산출물 생성
  - `npx.cmd tsc --noEmit`: 성공
  - Prisma 운영 마이그레이션 적용 완료
  - Vercel 운영 배포 `dpl_324LB9tQPp3Y9CkqXdaULdy2wo3u` Ready 및 사용자 도메인 연결 확인
  - 실제 보고서 결과는 `calapp/docs/reports/inventory-audit-2026-08-01-to-2026-11-30.md`에 기록
- 구현·문서 커밋: `d2672fa`, `0105d8c`, `f1f50a2`, `44d1036`

## 2026-08-01 — 예약 검증 재실행 방지·중지·사이트 실패 보고

- 요청 원문: "검증완료시 다시 검증 안하도록 하고, 네이버 토요일 201호·202호 단독으로 예약 가능인 것도 확인 필요로 안내해 달라. 검증 실패 사이트는 원인과 결과를 화면에 표시해 수작업 확인할 수 있게 하고, 실행 중인 창은 가능하면 백그라운드로 처리해 달라."
- 결과 요약:
  - 캘린더 버튼은 보고서 화면만 열며 자동 검증을 시작하지 않는다.
  - 동일 기간 완료 보고서는 새로 검증하지 않고 기존 결과를 재사용한다.
  - 명시적 시작 확인과 검증 중지 기능을 추가했다. 중지는 예약 동기화 워커에 영향을 주지 않는다.
  - 네이버 토요일 201호·202호 단독 상품이 열려 있으면 `확인 필요`로 보고한다.
  - 사이트별 완료·오류·중지 건수와 대표 실패 원인을 결과 화면에 표시한다.
  - 의도치 않게 시작된 검증 #3을 1,014/1,848건 시점에 중지했다.
- 변경 파일:
  - `calapp/app/admin/(dashboard)/calendar/page.tsx`
  - `calapp/app/admin/(dashboard)/inventory-audit/page.tsx`
  - `calapp/app/api/admin/inventory-audits/route.ts`
  - `calapp/app/api/admin/inventory-audits/[id]/route.ts`
  - `calapp/app/api/worker/inventory-audits/[id]/route.ts`
  - `calapp/lib/inventory-audits.ts`
  - `calapp/docs/inventory-audit.md`
- 검증:
  - `npx.cmd tsc --noEmit`: 성공
  - `npm.cmd run build`: 성공
  - 운영 API: 검증 #3 `cancelled`, 사이트 요약 4개 및 실패 원인 반환 확인
  - 동일 2026-08-01~2026-11-30 요청: 새 작업 없이 완료 보고서 #2 재사용 확인
  - Vercel 배포 `dpl_9ypQYD1H8UQErcCYhQn6cEVNq9aY` Ready
- 구현 커밋: `b255146`
